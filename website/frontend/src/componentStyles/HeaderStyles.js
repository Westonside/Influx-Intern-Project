import { makeStyles } from '@mui/styles';

const styles = makeStyles({
    searchBar:({
        marginLeft:'20px',
        
    }),
    bar:({
        color:'white !important'
    })
})

export default styles;